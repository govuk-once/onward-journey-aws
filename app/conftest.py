"""
DO NOT DELETE THIS FILE.

This file's presence at the root of the `app/` directory is required by pytest.
It acts as an anchor point, automatically adding the root  directory to Python's sys.path
so that absolute imports in the test suite resolve correctly without ModuleNotFound errors.
"""
