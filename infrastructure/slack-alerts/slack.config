# This config explicitly locks the state to the shared slack alerts key
key = "shared-infrastructure/slack-alerts.tfstate"

# Setting this to empty prevents terraform workspaces from appending a workspace name to the path, ensuring the VPC is always centrally managed.
workspace_key_prefix = ""
